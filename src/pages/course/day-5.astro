---
import CourseLayout from '../../layouts/CourseLayout.astro';

const sections = [
  { id: 'agents', label: 'What Is an Agent?' },
  { id: 'react', label: 'The ReAct Pattern' },
  { id: 'failure-modes', label: 'Failure Modes' },
  { id: 'evaluating-agents', label: 'Evaluating Agents' },
  { id: 'production', label: 'From Notebooks to Production' },
  { id: 'summary', label: 'Module Summary' },
];
---

<CourseLayout
  day={5}
  title="Agentic Workflows"
  description="Building autonomous research agents with tool use, the ReAct pattern, failure modes and safeguards, and the bridge from notebooks to production tools."
  sections={sections}
>

  <!-- ===================================================================
       OPENING FRAMING
       =================================================================== -->
  <section id="opening" class="opening-narrative">
    <p class="lead-paragraph">
      Over the past four days, you have built every piece of a research
      pipeline by hand: prompting, classifying, validating, extracting,
      retrieving, evaluating. Each step required you to decide what to do
      next. Today, you hand that decision-making to the model itself.
    </p>
    <p>
      An <strong>agent</strong> is an LLM wrapped in a loop. You give it
      a goal and a set of tools — the same functions you wrote in
      <a href="/course/day-4">Module 4</a> (search, extract, verify) — and
      it decides which tool to call, in what order, and what to do with
      the results. This is not a different kind of model. It is the same
      model, doing the same things, but orchestrating them autonomously.
    </p>
    <p>
      The power is obvious: a multi-step literature review that would take
      you hours can run in minutes. The risk is equally obvious: every
      failure mode from the course — hallucination, omission, distortion,
      retrieval errors, unfaithful generation — can now compound across
      steps without a human checking each one. Building agents that are
      useful <em>and</em> trustworthy is the challenge of this session.
    </p>

    <div class="key-takeaway">
      <p class="key-takeaway__label">After This Module You Will Be Able To</p>
      <ol>
        <li>Explain how an LLM agent works: tools, system prompt, parser, and execution loop.</li>
        <li>Build a ReAct agent that alternates between reasoning and tool use, and trace its decision-making step by step.</li>
        <li>Identify the four failure modes specific to agents (hallucination on missing data, format drift, redundant loops, cascading errors) and implement safeguards for each.</li>
        <li>Evaluate agent runs by outcome <em>and</em> by trajectory, connecting back to the faithfulness checks from <a href="/course/day-4">Module 4</a>.</li>
        <li>Decide when an agent is the wrong design &mdash; and where the autonomy line sits in a research workflow.</li>
        <li>Use production tools (Codespaces, Claude Code, MCP) to move from notebook prototypes to real research workflows.</li>
      </ol>
    </div>
  </section>

  <!-- ===================================================================
       SECTION 1: WHAT IS AN AGENT?
       =================================================================== -->
  <section id="agents">
    <h2>What Is an Agent?</h2>
    <p>
      In every session so far, <strong>you</strong> have been the
      decision-maker. You decided which prompt to write, which tool to call,
      which results to inspect. The model did what you asked, one step at a
      time.
    </p>
    <p>
      An <strong>agent</strong> reverses this. You give the model a goal,
      and it decides what steps to take: which tools to call, in what order,
      and what to do with the results. The model becomes the planner; you
      become the supervisor.
    </p>
    <p>
      This is not a different kind of model. It is the same LLM you have
      been using all week, wrapped in a loop that lets it take actions and
      observe results. The architecture is surprisingly simple: a system
      prompt that describes available tools, a parser that extracts tool
      calls from the model's output, and a while loop that executes tools
      and feeds results back. That's it.
    </p>

    <div class="definition-box">
      <p class="definition-box__label">Definition</p>
      <p class="definition-box__term">LLM Agent</p>
      <p>
        A system where a language model autonomously plans and executes
        multi-step tasks by calling external tools (search, code execution,
        APIs, databases) and using the results to inform subsequent actions.
        The model decides <em>what</em> to do and <em>when</em>; the
        surrounding code handles <em>how</em> (parsing, execution,
        error handling).
      </p>
    </div>

    <h3>Tools as Building Blocks</h3>
    <p>
      An agent is only as useful as its tools. In the notebook, the tools
      come directly from your Module 4 work: <code>search_literature()</code>
      searches the political science corpus using the FAISS index you built,
      <code>extract_findings()</code> pulls structured claims from a passage,
      <code>summarize()</code> condenses text, and
      <code>calculate()</code> evaluates mathematical expressions.
    </p>
    <p>
      Each tool is a Python function with a clear interface: it takes a
      string input and returns a string output. The agent does not know
      how the tools work internally. It knows only their names, their
      descriptions, and the format of their inputs. This separation is
      important: it means you can swap out the implementation (replace
      FAISS with a database, replace the local model with an API) without
      changing the agent's behaviour.
    </p>
    <p>
      The pattern scales. Production agents like
      <a href="https://docs.anthropic.com/en/docs/claude-code" target="_blank" rel="noopener">Claude Code</a>
      use the same architecture with more powerful tools: file system
      operations, shell commands, web search, code execution. The
      <a href="https://modelcontextprotocol.io" target="_blank" rel="noopener">Model Context Protocol (MCP)</a>
      standardises this further, providing a universal interface so that
      any tool can work with any model.
    </p>

    <div class="stop-and-think">
      <div class="stop-and-think__header">
        <svg class="stop-and-think__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span class="stop-and-think__label">Stop and Think</span>
      </div>
      <p>
        Think about your own research workflow. What are the
        &ldquo;tools&rdquo; you use repeatedly: searching a database,
        reading a paper, extracting data from a table, running a
        statistical test? If each of these were a function an agent
        could call, which parts of your workflow could be automated,
        and which require judgment that a model cannot provide?
      </p>
      <details>
        <summary>Reveal</summary>
        <div>
          <p>
            Most researchers find that the
            <em>search â†’ read â†’ extract â†’ organise</em> loop is highly
            automatable: an agent can find relevant papers, pull out key
            claims, and compile structured summaries. The parts that resist
            automation are theoretical judgment (is this framework
            appropriate?), methodological criticism (is this identification
            strategy valid?), and creative synthesis (what does this pattern
            mean?). The practical sweet spot is using agents for the
            mechanical parts and reserving human judgment for the
            interpretive parts.
          </p>
        </div>
      </details>
    </div>

    <div class="course-note">
      <svg class="course-note__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/>
      </svg>
      <p>
        <strong>In the notebook:</strong> Exercise 1a is &ldquo;Be the
        Algorithm&rdquo; for agents. You plan the steps a model would take
        to answer a research question, writing out the Thought / Action /
        Observation sequence by hand. Exercise 1b has you test each tool
        individually before putting them in the loop.
       <a href="https://colab.research.google.com/github/Oxford-LLMs-Research/Intro-to-LLMs-for-Social-Science/blob/main/course_notebooks/day_5_agents/day5_agentic_workflows.ipynb" target="_blank" rel="noopener">Open in Colab&nbsp;&rarr;</a></p>
    </div>
  </section>

  <!-- ===================================================================
       SECTION 2: THE REACT PATTERN
       =================================================================== -->
  <section id="react">
    <h2>The ReAct Pattern</h2>
    <p>
      Most LLM agents follow a framework called <strong>ReAct</strong>
      (<a href="https://arxiv.org/abs/2210.03629" target="_blank" rel="noopener">Yao et al., 2023</a>):
      the model alternates between <strong>Rea</strong>soning and
      <strong>Act</strong>ing. At each step, it produces a thought
      (reasoning about what to do next), then an action (a tool call),
      then observes the result. This cycle repeats until the model has
      enough information to produce a final answer.
    </p>

    <!-- ReAct loop interactive -->
    <div id="react-loop-root"></div>

    <p>
      The power of ReAct is transparency. Because the model writes out its
      reasoning at every step, you can audit the decision-making process.
      If the agent gives a wrong answer, you can trace back through the
      thoughts and observations to find where it went wrong: Did it search
      for the wrong thing? Did it misinterpret a tool result? Did it
      synthesize correctly but from incomplete evidence?
    </p>

    <div class="visual-diagram">
      <svg viewBox="0 0 460 340" width="460" height="340" xmlns="http://www.w3.org/2000/svg" style="max-width: 100%; height: auto; font-family: var(--font-body, Inter, system-ui, sans-serif); font-size: 11px;">
        <!-- Thought -->
        <rect x="150" y="30" width="140" height="40" rx="6" style="fill: var(--blue-9, #D5F5FD); stroke: var(--blue-2, #2272A8);" stroke-width="1.5"/>
        <text x="220" y="47" text-anchor="middle" style="fill: var(--blue-0, #1E3A5F);" font-weight="600">Thought</text>
        <text x="220" y="62" text-anchor="middle" style="fill: var(--text-muted, #6B7280);" font-size="9">what do I need next?</text>
        <line x1="220" y1="70" x2="220" y2="140" style="stroke: var(--text-dim, #9CA3AF);" stroke-width="1.5" marker-end="url(#d5reactArr)"/>
        <!-- Action -->
        <rect x="150" y="140" width="140" height="40" rx="6" style="fill: var(--blue-8, #B0ECF9); stroke: var(--blue-1, #1E5A8A);" stroke-width="1.5"/>
        <text x="220" y="157" text-anchor="middle" style="fill: var(--blue-0, #1E3A5F);" font-weight="600">Action</text>
        <text x="220" y="172" text-anchor="middle" style="fill: var(--text-muted, #6B7280);" font-size="9">a tool call</text>
        <!-- Tools -->
        <rect x="360" y="140" width="80" height="40" rx="6" style="fill: var(--surface, #F2F4F8); stroke: var(--border, #E2E6EE);" stroke-width="1.5"/>
        <text x="400" y="164" text-anchor="middle" style="fill: var(--text-muted, #6B7280);" font-weight="600">Tools</text>
        <line x1="290" y1="152" x2="360" y2="152" style="stroke: var(--text-dim, #9CA3AF);" stroke-width="1.5" marker-end="url(#d5reactArr)"/>
        <text x="325" y="144" text-anchor="middle" style="fill: var(--text-muted, #6B7280);" font-size="9">call</text>
        <path d="M 400,180 L 400,268 L 292,268" fill="none" style="stroke: var(--text-dim, #9CA3AF);" stroke-width="1.5" marker-end="url(#d5reactArr)"/>
        <text x="352" y="260" text-anchor="middle" style="fill: var(--text-muted, #6B7280);" font-size="9">result</text>
        <!-- Observation -->
        <rect x="150" y="248" width="140" height="40" rx="6" style="fill: var(--blue-9, #D5F5FD); stroke: var(--blue-2, #2272A8);" stroke-width="1.5"/>
        <text x="220" y="265" text-anchor="middle" style="fill: var(--blue-0, #1E3A5F);" font-weight="600">Observation</text>
        <text x="220" y="280" text-anchor="middle" style="fill: var(--text-muted, #6B7280);" font-size="9">what came back</text>
        <!-- Loop back -->
        <path d="M 150,268 C 60,268 60,50 150,50" fill="none" style="stroke: var(--accent-hover, #3BA3D4);" stroke-width="1.5" marker-end="url(#d5reactArrB)"/>
        <text x="48" y="160" text-anchor="middle" style="fill: var(--accent-hover, #3BA3D4);" font-size="9" transform="rotate(-90, 48, 160)">repeat until done</text>
        <!-- Final answer -->
        <path d="M 290,44 L 340,44" fill="none" style="stroke: var(--accent, #2D8ABF);" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#d5reactArrC)"/>
        <text x="315" y="34" text-anchor="middle" style="fill: var(--accent, #2D8ABF);" font-size="9">enough info?</text>
        <rect x="340" y="26" width="104" height="36" rx="6" style="fill: var(--accent-bg, #EBF7FD); stroke: var(--accent, #2D8ABF);" stroke-width="1.5"/>
        <text x="392" y="48" text-anchor="middle" style="fill: var(--blue-0, #1E3A5F);" font-weight="600">Final answer</text>
        <text x="150" y="322" style="fill: var(--text-dim, #9CA3AF);" font-size="10">The loop runs until the model can answer &#8212; or hits a step limit.</text>
        <defs>
          <marker id="d5reactArr" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" style="fill: var(--text-dim, #9CA3AF)"/></marker>
          <marker id="d5reactArrB" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" style="fill: var(--accent-hover, #3BA3D4)"/></marker>
          <marker id="d5reactArrC" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" style="fill: var(--accent, #2D8ABF)"/></marker>
        </defs>
      </svg>
      <p class="visual-diagram__caption">
        The ReAct loop. Each cycle produces a thought, an action against a
        tool, and an observation of the result, which feeds the next thought.
        The dashed exit runs whenever the model judges it has enough &#8212;
        and the step limit is the safeguard for when it never does.
      </p>
    </div>

    <h3>The Three Components</h3>
    <p>
      Building a ReAct agent requires three pieces, each with different
      levels of difficulty:
    </p>
    <p>
      <strong>The system prompt</strong> is the hardest part. It must
      describe the agent's role, list the available tools with precise
      descriptions, specify the exact Thought / Action / Action Input
      format, and set behavioural rules (one tool at a time, do not invent
      information, cite sources). If the format specification is imprecise,
      the model's output will be unparseable.
    </p>
    <p>
      <strong>The parser</strong> extracts tool calls from the model's text
      output. It looks for patterns like <code>Action: search_literature</code>
      and <code>Action Input: social media polarization</code>, or detects
      when the model produces a <code>Final Answer:</code> instead. This is
      string parsing, not conceptual work, but it must match the format
      specified in the system prompt exactly.
    </p>
    <p>
      <strong>The loop</strong> orchestrates everything. It sends the
      conversation to the model, parses the response, executes the
      requested tool, appends the observation to the conversation, and
      repeats. It also enforces safeguards: a maximum number of steps
      (preventing infinite loops), error handling for format violations,
      and cost tracking.
    </p>

    <div class="definition-box">
      <p class="definition-box__label">Definition</p>
      <p class="definition-box__term">ReAct (Reasoning + Acting)</p>
      <p>
        A prompting framework where the model alternates between reasoning
        (thinking through what to do next) and acting (executing a tool
        call), using the results of each action to inform subsequent
        reasoning. This creates a transparent, step-by-step
        problem-solving loop that can be audited and debugged.
      </p>
    </div>

    <h3>Text Parsing vs. Native Tool Calling</h3>
    <p>
      The notebook builds the ReAct loop from scratch: the model outputs
      text, and your code parses <code>Action: tool_name</code> from the
      response. This is pedagogically valuable because it exposes every
      moving part. But production systems rarely work this way.
    </p>
    <p>
      Modern APIs (OpenAI, Anthropic, Google) support
      <strong>native tool calling</strong>: you define tools as a JSON
      schema, the model returns a structured tool-call object rather than
      free text, and the API handles the format for you. This eliminates
      format drift entirely — the model cannot produce an unparseable
      response because the tool call is a structured object, not a
      substring in prose.
    </p>
    <p>
      Frameworks like
      <a href="https://langchain-ai.github.io/langgraph/" target="_blank" rel="noopener">LangGraph</a>
      and the
      <a href="https://docs.anthropic.com/en/docs/build-with-claude/tool-use" target="_blank" rel="noopener">Anthropic tool-use API</a>
      abstract this further, letting you define tools as Python functions
      and handling the serialisation, execution, and result injection
      automatically. The ReAct loop is still running underneath, but you
      no longer write the parser yourself.
    </p>

    <div class="course-note course-note--research">
      <svg class="course-note__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
      </svg>
      <p>
        <strong>Research Note.</strong>
        The notebook uses text-parsed ReAct intentionally: you need to see
        how the loop works to understand what can go wrong. Once you have
        built it by hand, switch to native tool calling for any real
        project. The failure modes (hallucination, cascading errors,
        redundant loops) remain the same regardless of whether the tool
        call is parsed from text or returned as a structured object.
        Format drift is the one failure mode that native APIs eliminate.
      </p>
    </div>

    <div class="stop-and-think">
      <div class="stop-and-think__header">
        <svg class="stop-and-think__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span class="stop-and-think__label">Stop and Think</span>
      </div>
      <p>
        The system prompt for a ReAct agent is much longer and more
        prescriptive than a classification prompt. Why? What happens if the
        format specification is vague?
      </p>
      <details>
        <summary>Reveal</summary>
        <div>
          <p>
            A classification prompt produces a single output (a label).
            An agent prompt produces <em>structured</em> output that must
            be parsed by code. If the model writes
            <code>I'll search for: social media</code> instead of
            <code>Action: search_literature</code>, the parser fails and
            the loop breaks. The system prompt must be prescriptive enough
            that the model produces machine-parseable output reliably across
            many steps. This is why larger models are better for agents:
            they follow structured formats more consistently.
          </p>
        </div>
      </details>
    </div>

    <div class="course-note">
      <svg class="course-note__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/>
      </svg>
      <p>
        <strong>In the notebook:</strong> Exercise 2a has you write the
        system prompt. Exercise 2b has you build the agent loop. Exercise
        2c runs the agent on real research questions so you can watch it
        reason, act, and synthesize in real time.
       <a href="https://colab.research.google.com/github/Oxford-LLMs-Research/Intro-to-LLMs-for-Social-Science/blob/main/course_notebooks/day_5_agents/day5_agentic_workflows.ipynb" target="_blank" rel="noopener">Open in Colab&nbsp;&rarr;</a></p>
    </div>
  </section>

  <!-- ===================================================================
       SECTION 3: FAILURE MODES
       =================================================================== -->
  <section id="failure-modes">
    <h2>When Agents Fail</h2>
    <p>
      Agents fail in ways that are qualitatively different from single-call
      LLM failures. A classification error affects one label. An agent
      error can cascade through multiple steps, producing a
      confident-sounding synthesis built on flawed intermediate results.
      Understanding these failure modes is essential for building agents
      you can trust.
    </p>

    <!-- Agent failure modes reference -->
    <div id="failure-modes-root"></div>

    <h3>Hallucination on Missing Data</h3>
    <p>
      When the corpus does not contain information relevant to the query,
      a well-behaved agent should say so. Instead, many agents fabricate
      plausible-sounding results. The agent might &ldquo;search&rdquo; the
      corpus, get back irrelevant passages, and then synthesize an answer
      as if the passages contained what it needed. The final answer reads
      as confidently grounded, but the connection between the retrieved
      text and the claims is fabricated.
    </p>
    <p>
      This is more dangerous than single-call hallucination because the
      agent <em>did</em> use a tool. The presence of a search step creates
      a false sense of grounding. The answer looks like it went through
      proper retrieval when it did not.
    </p>

    <h3>Format Drift</h3>
    <p>
      After several steps, the model may drift out of the
      Thought / Action / Observation format. It might start writing
      conversational prose, skip the Action line, or merge its reasoning
      with a premature final answer. When the parser cannot extract a tool
      call, the loop stalls or produces an error.
    </p>
    <p>
      Format drift is more common with smaller models and longer
      conversations. The accumulated context pushes the model away from
      the structured format established in the system prompt. Mitigation
      strategies include keeping the conversation short (fewer steps),
      re-injecting format reminders, or using models specifically trained
      for tool calling.
    </p>

    <h3>Infinite Loops and Redundant Actions</h3>
    <p>
      An agent might search for the same query repeatedly, extracting
      from the same passage multiple times, or cycling between tools
      without making progress. This wastes tokens and money without
      improving the answer. A hard step limit (typically 5-10 steps)
      prevents runaway costs, but it does not prevent the agent from
      wasting those steps on redundant actions.
    </p>

    <h3>Cascading Errors</h3>
    <p>
      An early mistake propagates through subsequent steps. If the
      agent's first search retrieves irrelevant passages, the extraction
      step will extract irrelevant claims, and the synthesis will build
      on irrelevant evidence. Each step looks locally reasonable (the
      extraction is faithful to the passage it was given), but the chain
      is globally wrong because the first link was bad.
    </p>
    <p>
      This is the agent-specific version of the &ldquo;retrieval quality
      is the bottleneck&rdquo; lesson from Module 4. In an agent, every
      tool call is a potential bottleneck, and errors compound.
    </p>

    <h3>Safeguards</h3>
    <p>
      Production agents address these failure modes with several
      mechanisms:
    </p>
    <p>
      <strong>Step budgets</strong> cap the number of tool calls. After
      reaching a limit, the agent must synthesize from whatever it has.
      Some implementations inject a warning at step N-2: &ldquo;You have
      2 steps remaining. Summarize your findings.&rdquo;
    </p>
    <p>
      <strong>Minimum evidence rules</strong> require the agent to search
      at least twice before producing a final answer, preventing premature
      synthesis from a single source.
    </p>
    <p>
      <strong>Provenance verification</strong> runs after the agent
      finishes: a separate call checks whether every claim in the final
      answer is supported by an observation from a tool call. This is
      the same faithfulness check from
      <a href="/course/day-4">Module 4</a>, applied to agent output. The
      <a href="/course/day-4#evaluation">three-layer evaluation
      workflow</a> (automated metrics, stratified manual review, domain
      expert audit) extends naturally to agents: run RAGAS-style metrics
      over the agent&rsquo;s final answers, manually audit a sample of
      the reasoning traces, and verify any claim that will be published.
    </p>
    <p>
      <strong>Human-in-the-loop checkpoints</strong> pause the agent at
      critical decision points and ask for human approval before
      proceeding. For high-stakes applications (modifying data, sending
      communications), this is essential.
    </p>

    <div class="stop-and-think">
      <div class="stop-and-think__header">
        <svg class="stop-and-think__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span class="stop-and-think__label">Stop and Think</span>
      </div>
      <p>
        An agent answers your research question with a well-structured
        synthesis citing three sources. The provenance check shows that
        all three citations correspond to real passages in the corpus.
        Does this mean the answer is trustworthy?
      </p>
      <details>
        <summary>Reveal</summary>
        <div>
          <p>
            Not necessarily. Provenance confirms that the citations are
            real, but it does not confirm that the agent's
            <em>interpretation</em> of those passages is correct. The agent
            may have distorted the meaning (the same failure mode from
            Module 4's extraction section), selectively quoted to support a
            conclusion the passages do not actually support, or missed
            contradicting evidence because it did not search broadly enough.
            Grounded citations are a necessary condition for trust, not a
            sufficient one.
          </p>
        </div>
      </details>
    </div>

    <div class="course-note">
      <svg class="course-note__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/>
      </svg>
      <p>
        <strong>In the notebook:</strong> Exercise 2d stress-tests the agent
        with out-of-domain questions, adversarial inputs, and questions
        designed to trigger failure modes. Exercise 2e has you pick one
        safeguard and implement it: minimum evidence rules, step budget
        awareness, or post-hoc source verification.
       <a href="https://colab.research.google.com/github/Oxford-LLMs-Research/Intro-to-LLMs-for-Social-Science/blob/main/course_notebooks/day_5_agents/day5_agentic_workflows.ipynb" target="_blank" rel="noopener">Open in Colab&nbsp;&rarr;</a></p>
    </div>

    <div class="key-takeaway">
      <p class="key-takeaway__label">Key Takeaway</p>
      <p>
        An agent is a loop, not magic. The quality depends on three things:
        the model's ability to follow the Thought/Action format reliably,
        the quality and coverage of the tools, and safeguards against the
        failure modes catalogued above. In production, you add retry logic,
        context management, cost tracking, and human-in-the-loop approval
        for high-stakes actions.
      </p>
    </div>

    <h3>Resources</h3>
    <ul class="resource-list">
      <li><a href="https://arxiv.org/abs/2210.03629" target="_blank" rel="noopener">Yao et al. (2023), <em>ReAct: Synergizing Reasoning and Acting in Language Models</em></a>: the foundational framework for language model agents.</li>
      <li><a href="https://arxiv.org/abs/2302.04761" target="_blank" rel="noopener">Schick et al. (2023), <em>Toolformer: Language Models Can Learn to Use Tools</em></a>: how models learn to call external tools during generation.</li>
      <li><a href="https://arxiv.org/abs/2308.11432" target="_blank" rel="noopener">Wang et al. (2023), <em>A Survey on Large Language Model based Autonomous Agents</em></a>: comprehensive overview of agent architectures, planning strategies, and evaluation.</li>
    </ul>
  </section>

  <!-- ===================================================================
       SECTION 4: EVALUATING AGENTS
       =================================================================== -->
  <section id="evaluating-agents">
    <h2>Evaluating Agents</h2>
    <p>
      Every method in this course arrived with an evaluation protocol:
      classification got Cohen&rsquo;s &kappa; and a validation sample
      (<a href="/course/day-3">Module 3</a>), extraction got faithfulness
      checks, RAG got separate retrieval and generation metrics
      (<a href="/course/day-4">Module 4</a>). Agents are the hardest case,
      because the thing you built does not just produce an answer &mdash;
      it chooses its own path to the answer. Evaluating only the answer
      leaves the path unexamined, and the path is where agents fail.
    </p>

    <h3>Trajectories, Not Just Outcomes</h3>
    <p>
      An agent can reach the right answer for the wrong reasons. It can
      skip the literature search, hallucinate a plausible summary, and
      happen to be correct; it can retrieve the wrong documents and be
      rescued by the model&rsquo;s background knowledge. On a ten-question
      demo, this looks like success. At scale, on questions where you
      cannot check by eye, the same behaviour is silent fabrication.
      Outcome accuracy alone cannot tell these cases apart.
    </p>
    <p>
      The fix is <strong>trajectory review</strong>: reading the full
      Thought&thinsp;/&thinsp;Action&thinsp;/&thinsp;Observation trace
      against a short rubric, the same way you read a coder&rsquo;s
      reasoning in qualitative work. Three questions cover most of it.
      Did the agent take a defensible path &mdash; the tools a competent
      research assistant would have chosen, in a sensible order? Is every
      claim in the final answer grounded in an observation actually
      returned by a tool, rather than imported from the model&rsquo;s
      prior knowledge? And did it stop for the right reason &mdash;
      because it had enough evidence, not because it hit the step limit?
    </p>
    <p>
      Two practices make this manageable at research scale. First, keep a
      <strong>fixed task battery</strong>: a set of questions with known
      answers and known-good trajectories, re-run after <em>any</em>
      change to the prompt, the model, or the tools. An agent is a
      pipeline, and pipelines regress; the battery is your regression
      test. Second, track <strong>cost per episode</strong> alongside
      accuracy. Two agents with equal accuracy are not equal if one takes
      three tool calls and the other takes nineteen &mdash; and a rising
      average step count is often the first visible symptom of a prompt
      change that broke something subtle.
    </p>

    <div class="stop-and-think">
      <div class="stop-and-think__header">
        <svg class="stop-and-think__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span class="stop-and-think__label">Stop and Think</span>
      </div>
      <p>
        Your agent answers a question correctly, but its trace shows it
        never ran the search tool &mdash; the answer came entirely from
        the model&rsquo;s parametric knowledge. Does this count as a
        success? What would you need to believe about the next hundred
        questions for that answer to be trustworthy?
      </p>
      <details>
        <summary>Reveal</summary>
        <div>
          <p>
            It is a failure dressed as a success. The workflow you designed
            &mdash; search, then ground the answer in what was found &mdash;
            did not run; you got a lucky draw from the model&rsquo;s
            training data instead. Trusting the next hundred questions
            means believing the model&rsquo;s background knowledge is
            complete and current for your entire domain, which is exactly
            the assumption RAG and tool use exist to avoid. Grounding
            failures with correct answers are the most dangerous kind,
            because outcome metrics will never surface them.
          </p>
        </div>
      </details>
    </div>

    <h3>Orchestration: When Not to Use an Agent</h3>
    <p>
      There is a spectrum between the fixed pipelines of
      <a href="/course/day-3">Module 3</a> and the free agent of this
      module, and most research work belongs somewhere on it. Three
      patterns cover the practical range. A <strong>fixed pipeline</strong>
      calls the model at predetermined steps &mdash; classify, then
      extract, then verify &mdash; with control flow you wrote and can
      audit. A <strong>generator&ndash;judge</strong> design adds a second
      model call that checks the first&rsquo;s output against the source
      before it is accepted: a quality gate, not an autonomous loop. A
      full <strong>agent</strong> lets the model write the control flow
      itself at runtime.
    </p>
    <p>
      The useful heuristic: <strong>an agent is a pipeline that writes
      itself &mdash; so if you can write the pipeline in advance, write
      it.</strong> A fixed pipeline is cheaper (no reasoning tokens spent
      deciding what to do), reproducible (the same input takes the same
      path every run), and auditable (a reviewer can read your control
      flow as code). Reserve genuine autonomy for tasks where the path
      honestly varies item by item &mdash; a literature search whose next
      query depends on what the last one found &mdash; and even then,
      constrain it: a small tool set, a tight step limit, and a judge at
      the end. Autonomy is a cost you pay for flexibility, not a feature
      to maximise.
    </p>

    <h3>The Autonomy Line</h3>
    <p>
      Every agentic research workflow needs an explicit answer to one
      question: <strong>what may run unattended, and what may not?</strong>
      On the safe side of the line sit tasks whose failures are cheap and
      checkable &mdash; reformatting, deduplication, retrieval, first-pass
      screening that a human will review anyway. On the far side sits
      anything that enters the paper: reported quantities, sampled cases,
      substantive interpretations. The rule from
      <a href="/course/day-3">Module 3</a> does not soften because the
      pipeline became autonomous &mdash; the agent is another coder, and
      no coder&rsquo;s output enters your results unvalidated.
    </p>
    <p>
      Treat the line as a design decision you write down, like a codebook
      rule: which steps run unattended, which require review, and what
      the validation plan is for anything the agent touched. A reviewer
      who asks &ldquo;how do you know the agent did this correctly?&rdquo;
      should get the same quality of answer as one asking about a human
      research assistant &mdash; a protocol, a sample, an agreement
      statistic, not reassurance.
    </p>

    <div class="key-takeaway">
      <p class="key-takeaway__label">Key Takeaway</p>
      <p>
        Evaluate the path, not just the answer: trajectory review, a fixed
        regression battery, and per-episode cost tracking are the agent
        equivalents of the validation protocol you built in Module 3.
        Prefer the least autonomy that does the job &mdash; and wherever
        the agent&rsquo;s output enters your findings, it is another
        coder, subject to the same validation as any other.
      </p>
    </div>

    <h3>Resources</h3>
    <ul class="resource-list">
      <li><a href="https://arxiv.org/abs/2407.01502" target="_blank" rel="noopener">Kapoor et al. (2024), <em>AI Agents That Matter</em></a>: argues that agent benchmarks overstate capability by ignoring cost and reproducibility &mdash; the evaluation stance this section takes.</li>
      <li><a href="https://arxiv.org/abs/2308.03688" target="_blank" rel="noopener">Liu et al. (2023), <em>AgentBench: Evaluating LLMs as Agents</em></a>: systematic multi-environment agent evaluation, and a template for building task batteries.</li>
    </ul>
  </section>

  <!-- ===================================================================
       SECTION 5: FROM NOTEBOOKS TO PRODUCTION
       =================================================================== -->
  <section id="production">
    <h2>From Notebooks to Production</h2>
    <p>
      The agent you build in the notebook works, but it lives in a Colab
      environment. Production agents need a real development environment
      (a terminal, file system, version control), persistent tool
      connections (databases, APIs, web search), better observability
      (logging, cost tracking, auditing), and human oversight for
      high-stakes actions.
    </p>
    <p>
      Three tools bridge this gap. Your instructor will demonstrate each
      one live during the session.
    </p>

    <h3>GitHub Codespaces</h3>
    <p>
      <a href="https://github.com/features/codespaces" target="_blank" rel="noopener">Codespaces</a>
      provides a full cloud development environment in your browser:
      VS Code with a terminal, file system, Git, Python, and anything
      else you need. No local installation required.
    </p>
    <p>
      For agent development, Codespaces solves the &ldquo;works on my
      machine&rdquo; problem. You configure the environment once (in a
      <code>devcontainer.json</code> file), and every collaborator gets
      an identical setup. GitHub's free tier includes 60 hours per month,
      which is sufficient for experimentation.
    </p>

    <h3>Claude Code</h3>
    <p>
      <a href="https://docs.anthropic.com/en/docs/claude-code" target="_blank" rel="noopener">Claude Code</a>
      is a command-line agent that lives in your terminal. It can read and
      write files, execute shell commands, search codebases, and plan
      multi-step tasks. It uses the exact same ReAct architecture you
      built in the notebook, with a frontier model and real filesystem
      tools.
    </p>
    <p>
      For researchers, Claude Code is useful for data analysis
      (&ldquo;Load this CSV, compute summary statistics, and create a
      visualization&rdquo;), pipeline building (&ldquo;Write a Python
      script that cleans and merges these three datasets&rdquo;), and
      iterative analysis (&ldquo;The regression shows heteroskedasticity.
      Add robust standard errors and re-run&rdquo;).
    </p>
    <p>
      Installation requires Node.js and an Anthropic API key:
    </p>
    <pre style="background:var(--surface-alt, #F5F5F0);border:1px solid var(--border, #E2E6EE);border-radius:8px;padding:0.75rem 1rem;font-family:var(--font-mono, monospace);font-size:0.82rem;color:var(--text-sec, #3D4455);overflow-x:auto;">npm install -g @anthropic-ai/claude-code
cd my-research-project/
claude</pre>

    <h3>Model Context Protocol (MCP)</h3>
    <p>
      <a href="https://modelcontextprotocol.io" target="_blank" rel="noopener">MCP</a>
      is an open standard that lets AI models connect to external tools
      and data sources. Instead of defining tools as Python functions
      (as you do in the notebook), MCP defines a protocol so that any tool
      can work with any model.
    </p>
    <p>
      Think of it as <strong>USB for AI</strong>: just as USB lets any
      peripheral work with any computer, MCP lets any tool work with any
      AI model. You can connect Claude to your university's database,
      Google Drive, or Zotero. You can build custom MCP servers that
      expose your datasets or analysis pipelines. And you can share tools
      across your research group: one person builds the server, everyone
      uses it.
    </p>
    <p>
      MCP servers are lightweight programs (often 50-100 lines of Python
      or TypeScript). The
      <a href="https://modelcontextprotocol.io" target="_blank" rel="noopener">MCP documentation</a>
      has quickstart guides and examples.
    </p>

    <div class="course-note">
      <svg class="course-note__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
      </svg>
      <p>
        <strong>Anthropic (2024),</strong>
        <a href="https://modelcontextprotocol.io" target="_blank" rel="noopener"><em>Model Context Protocol Specification.</em></a>
        Open standard for connecting AI models to external tools and
        data sources. The specification defines how tools advertise their
        capabilities, how models request tool calls, and how results are
        returned.
      </p>
    </div>

    <div class="key-takeaway">
      <p class="key-takeaway__label">Key Takeaway</p>
      <p>
        The gap between a notebook prototype and a production workflow is
        tooling, not theory. Codespaces give you a reproducible
        environment, Claude Code puts a frontier agent in your terminal,
        and MCP lets you connect any model to any data source. The agent
        architecture is the same in all three: tools, a loop, and
        safeguards. Start with the notebook, graduate to production tools
        when the task outgrows a single session.
      </p>
    </div>

    <h3>Resources</h3>
    <ul class="resource-list">
      <li><a href="https://docs.anthropic.com/en/docs/claude-code" target="_blank" rel="noopener">Claude Code documentation</a>: setup, usage, and best practices for terminal-based AI assistance.</li>
      <li><a href="https://github.com/features/codespaces" target="_blank" rel="noopener">GitHub Codespaces</a>: browser-based development environments with free tier (60h/month).</li>
      <li><a href="https://modelcontextprotocol.io" target="_blank" rel="noopener">Model Context Protocol</a>: open standard for connecting AI models to external tools.</li>
      <li><a href="https://langchain-ai.github.io/langgraph/" target="_blank" rel="noopener">LangGraph</a>: framework for building stateful, multi-step agent workflows.</li>
      <li><a href="https://elicit.com" target="_blank" rel="noopener">Elicit</a> and <a href="https://www.semanticscholar.org" target="_blank" rel="noopener">Semantic Scholar</a>: AI-powered research tools for literature review that use agent-like architectures under the hood.</li>
    </ul>
  </section>

  <!-- ===================================================================
       MODULE SUMMARY
       =================================================================== -->
  <section id="summary" class="opening-narrative">
    <h2>Module Summary</h2>
    <p>
      This module introduced the agent: an LLM wrapped in a loop that
      decides what to do, executes tools, and uses the results to plan
      its next step. The <strong>ReAct pattern</strong> (Thought / Action /
      Observation) makes this decision-making transparent and auditable.
      Modern APIs replace the text-parsed version with native tool calling,
      but the underlying loop is the same.
    </p>
    <p>
      <strong>Failure modes</strong> in agents are qualitatively different
      from single-call errors: hallucination on missing data looks
      grounded because a search step occurred, format drift breaks the
      loop silently, redundant actions waste budget, and cascading errors
      propagate early mistakes through every subsequent step. Safeguards
      (step budgets, minimum evidence rules, provenance verification,
      human-in-the-loop checkpoints) exist for each failure mode. None
      are optional for production use.
    </p>
    <p>
      <strong>Production tools</strong> bridge the gap from notebooks to
      real workflows: Codespaces for reproducible environments, Claude Code
      for terminal-based agents, and MCP for connecting models to your
      data. These tools use the same architecture you built by hand, with
      more capable models and better safeguards.
    </p>

    <h3>The Full Arc: Five Modules, One Toolkit</h3>
    <p>
      The progression across the course has been deliberate.
      <a href="/course/day-1">Module 1</a> built the foundations: how models
      represent meaning and generate text.
      <a href="/course/day-2">Modules 2</a> and
      <a href="/course/day-3">3</a> taught you to control models:
      prompting, validation, fine-tuning, APIs at scale.
      <a href="/course/day-4">Module 4</a> applied those skills to harder
      research tasks: extraction, RAG, and rigorous evaluation.
      Module 5 showed how to orchestrate all of these into autonomous
      workflows.
    </p>
    <p>
      Each module&rsquo;s tools became the next module&rsquo;s building blocks.
      Module 1&rsquo;s embeddings powered Module 4&rsquo;s retrieval.
      Module 2&rsquo;s prompting skills shaped Module 4&rsquo;s extraction
      prompts and Module 5&rsquo;s system prompt. Module 3&rsquo;s validation
      framework carried through as the faithfulness checks in Modules 4
      and 5. Module 4&rsquo;s evaluation metrics (RAGAS, provenance,
      manual review) became Module 5&rsquo;s agent safeguards. And
      Module 4&rsquo;s functions became Module 5&rsquo;s agent tools.
    </p>

    <div class="stop-and-think">
      <div class="stop-and-think__header">
        <svg class="stop-and-think__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span class="stop-and-think__label">Stop and Think</span>
      </div>
      <p>
        Think about a research question from your own work. How could you
        decompose it into a multi-step pipeline using the techniques covered
        in this course? Which modules&rsquo; tools would each step draw on?
      </p>
    </div>

    <h3>Where to Go from Here</h3>
    <p>
      The course gave you foundations. Here are concrete next steps, ordered
      by how quickly you can start:
    </p>
    <p>
      <strong>This week:</strong> Apply the RAG pipeline from
      <a href="/course/day-4">Module 4</a> to your own corpus (parliamentary
      records, news archives, legal texts). Build a classification
      pipeline from <a href="/course/day-3">Module 3</a> for your own
      annotation task. Try Claude Code on a real research project.
    </p>
    <p>
      <strong>This month:</strong> Set up a Codespace for your research
      group&rsquo;s codebase. Build a custom MCP server that connects
      Claude to your data. Run a validation study comparing LLM
      annotations to human annotations on your specific task.
    </p>
    <p>
      <strong>This year:</strong> Integrate LLM-based methods into a
      published paper. Contribute to the methodological conversation about
      validity, reproducibility, and best practices for LLMs in social
      science.
    </p>

    <div class="key-takeaway">
      <p class="key-takeaway__label">Key Takeaway</p>
      <p>
        Agents can automate significant portions of the research workflow:
        literature reviews, data collection pipelines, iterative analysis.
        The key is understanding what to delegate and what requires human
        judgment. Start with well-defined, bounded tasks (literature search
        and extraction) before attempting open-ended research assistance.
        Build safeguards in from the start, not after something goes wrong.
        And remember the thread that ran through every day of this course:
        <strong>the model is an instrument, and instruments require
        calibration</strong>.
      </p>
    </div>

    <div class="course-note">
      <svg class="course-note__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/>
      </svg>
      <p>
        <strong>Stay connected.</strong> All notebooks and materials remain
        available at
        <a href="https://llmsforsocialscience.net" target="_blank" rel="noopener">llmsforsocialscience.net</a>.
        If you are interested in doing research on LLMs for social science,
        reach out: we are building a research lab focused on these methods.
      </p>
    </div>
  </section>

</CourseLayout>

<!-- =================================================================== -->
<!-- COMPONENT STYLES                                                    -->
<!-- =================================================================== -->
<style is:inline>
/* .widget-wrap / -eyebrow / -title / -intro live in global.css */

/* â”€â”€ ReAct loop â”€â”€ */
.rl__question { background: var(--surface-alt, #F5F5F0); border: 1px solid var(--border, #E2E6EE); border-radius: 8px; padding: 0.85rem 1.15rem; margin-bottom: 1.5rem; }
.rl__question-label { font-family: var(--font-mono, monospace); font-size: 0.62rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.06em; color: var(--text-muted, #6B7280); margin-bottom: 0.3rem; }
.rl__question-text { font-size: 0.92rem; color: var(--text, #1A1D27); font-weight: 500; font-style: italic; line-height: 1.5; }

.rl__trace { position: relative; padding-left: 2rem; min-height: 10rem; }
.rl__trace::before { content: ''; position: absolute; left: 0.55rem; top: 0; bottom: 0; width: 2px; background: var(--border, #E2E6EE); }

.rl__step { position: relative; margin-bottom: 0.85rem; animation: rlStepIn 0.35s ease-out; }
@keyframes rlStepIn { from { opacity: 0; transform: translateX(-8px); } to { opacity: 1; transform: translateX(0); } }

.rl__step-dot { position: absolute; left: -1.65rem; top: 0.55rem; width: 12px; height: 12px; border-radius: 50%; border: 2px solid white; z-index: 1; }
.rl__step-dot--thought { background: #2E5090; box-shadow: 0 0 0 1px #B8CCE8; }
.rl__step-dot--action { background: #8B6914; box-shadow: 0 0 0 1px #E8D49A; }
.rl__step-dot--observation { background: #2D7D46; box-shadow: 0 0 0 1px #B8DBBD; }
.rl__step-dot--answer { background: #6D28D9; box-shadow: 0 0 0 1px #C9B5F5; }

.rl__step-card { border-radius: 8px; padding: 0.7rem 0.9rem; font-size: 0.82rem; line-height: 1.6; color: var(--text-sec, #3D4455); }
.rl__step-card--thought { background: #EDF2FA; border: 1px solid #B8CCE8; }
.rl__step-card--action { background: #FDF8EC; border: 1px solid #E8D49A; }
.rl__step-card--observation { background: #EDF7F0; border: 1px solid #B8DBBD; }
.rl__step-card--answer { background: #F3EEFF; border: 1px solid #C9B5F5; }

.rl__step-label { font-family: var(--font-mono, monospace); font-size: 0.62rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 0.25rem; }
.rl__step-label--thought { color: #2E5090; }
.rl__step-label--action { color: #8B6914; }
.rl__step-label--observation { color: #2D7D46; }
.rl__step-label--answer { color: #6D28D9; }

.rl__step-card code { font-family: var(--font-mono, monospace); font-size: 0.8em; background: rgba(255,255,255,0.6); border-radius: 3px; padding: 0.1em 0.3em; }

.rl__controls { display: flex; align-items: center; gap: 0.75rem; margin-top: 1.25rem; padding-top: 1rem; border-top: 1px solid var(--border, #E2E6EE); }
.rl__btn { font-family: var(--font-body, sans-serif); font-size: 0.82rem; font-weight: 600; padding: 0.55rem 1.25rem; border-radius: 8px; border: none; cursor: pointer; transition: all 0.25s ease; }
.rl__btn--primary { background: var(--accent, #2D8ABF); color: white; }
.rl__btn--primary:hover { background: #1E3A6E; }
.rl__btn--ghost { background: transparent; color: var(--accent, #2D8ABF); border: 1.5px solid var(--border, #E2E6EE); }
.rl__btn--ghost:hover { border-color: var(--accent, #2D8ABF); background: #E8EEF6; }
.rl__step-counter { font-family: var(--font-mono, monospace); font-size: 0.72rem; color: var(--text-muted, #6B7280); margin-left: auto; }

.rl__legend { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: 1rem; font-size: 0.72rem; color: var(--text-muted, #6B7280); }
.rl__legend-item { display: flex; align-items: center; gap: 0.3rem; }
.rl__legend-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }

.rl__annotation { margin-top: 1.25rem; padding: 0.85rem 1rem; border-left: 3px solid var(--accent, #2D8ABF); background: #E8EEF6; border-radius: 0 8px 8px 0; font-size: 0.85rem; color: var(--text-sec, #3D4455); line-height: 1.6; min-height: 3rem; }
.rl__annotation strong { color: var(--text, #1A1D27); }

/* â”€â”€ Failure modes â”€â”€ */
.fm__cards { display: flex; flex-direction: column; gap: 0.5rem; }
.fm__card { background: var(--surface, #FFF); border: 1.5px solid var(--border, #E2E6EE); border-radius: 12px; overflow: hidden; transition: border-color 0.25s ease; }
.fm__card-header { display: flex; align-items: center; gap: 0.75rem; padding: 0.85rem 1.15rem; cursor: pointer; user-select: none; transition: background 0.25s ease; }
.fm__card-header:hover { background: var(--surface-alt, #F5F5F0); }
.fm__card-icon { width: 2rem; height: 2rem; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 0.9rem; flex-shrink: 0; }
.fm__card-label { flex: 1; }
.fm__card-name { font-size: 0.9rem; font-weight: 700; color: var(--text, #1A1D27); line-height: 1.3; }
.fm__card-brief { font-size: 0.75rem; color: var(--text-muted, #6B7280); line-height: 1.4; margin-top: 0.1rem; }
.fm__card-chevron { font-size: 0.7rem; color: var(--text-muted, #6B7280); transition: transform 0.25s ease; flex-shrink: 0; }
.fm__card--active .fm__card-chevron { transform: rotate(90deg); }
.fm__card-detail { display: none; padding: 0 1.15rem 1.15rem 1.15rem; }
.fm__card--active .fm__card-detail { display: block; animation: fmFadeIn 0.3s ease-out; }
@keyframes fmFadeIn { from { opacity: 0; } to { opacity: 1; } }
.fm__example { border-radius: 8px; padding: 0.85rem; margin-bottom: 0.75rem; font-size: 0.82rem; line-height: 1.6; color: var(--text-sec, #3D4455); }
.fm__example-label { font-family: var(--font-mono, monospace); font-size: 0.6rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 0.35rem; }
.fm__trace-line { display: flex; align-items: flex-start; gap: 0.5rem; margin-bottom: 0.3rem; font-size: 0.78rem; }
.fm__trace-label { font-family: var(--font-mono, monospace); font-size: 0.62rem; font-weight: 700; text-transform: uppercase; padding: 0.1rem 0.35rem; border-radius: 3px; flex-shrink: 0; margin-top: 0.15rem; white-space: nowrap; }
.fm__safeguard { background: #EDF7F0; border: 1px solid #B8DBBD; border-radius: 8px; padding: 0.7rem 0.85rem; font-size: 0.82rem; line-height: 1.55; color: var(--text-sec, #3D4455); }
.fm__safeguard-label { font-family: var(--font-mono, monospace); font-size: 0.6rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em; color: #2D7D46; margin-bottom: 0.25rem; }
</style>

<!-- =================================================================== -->
<!-- 1. REACT LOOP VISUALISER                                            -->
<!-- =================================================================== -->
<script is:inline>
(function () {
  var root = document.getElementById("react-loop-root");
  if (!root) return;

  var question = "What does recent political science research say about the relationship between social media and political polarization?";

  var steps = [
    { type: "thought", text: "I need to find what the political science literature says about social media and polarization. Let me start with a broad search.",
      annotation: "<strong>Step 1: Planning.</strong> The agent reasons about the goal before acting. This is the 'Rea' in ReAct. A direct answer without searching would risk hallucination." },
    { type: "action", text: '<code>search_literature("social media political polarization")</code>',
      annotation: "<strong>Step 1: Acting.</strong> The agent calls its search tool. Your code parses this line, extracts the function name and argument, and executes the function." },
    { type: "observation", text: '[1] "Kubin & von Sikorski (2024): Research shows mixed evidence on whether social media directly causes polarization. Some studies find echo chamber effects, while others emphasize that most users are exposed to cross-cutting content..." (score: 0.84)<br>[2] "Settle (2022): Political communication on social media differs from offline because of algorithmic curation, low barriers to expression, and the public nature of interactions..." (score: 0.79)<br>[3] "Barber\u00e1 (2023): Network analysis of Twitter suggests ideological sorting has increased over time, but the causal role of the platform versus broader societal trends remains debated..." (score: 0.72)',
      annotation: "<strong>Step 1: Observing.</strong> The tool returns results. These are real passages from the corpus, not model-generated text. The agent now has evidence to work with." },
    { type: "thought", text: "I found some relevant results about mixed evidence and echo chambers. But I should search for experimental evidence on algorithmic effects specifically.",
      annotation: "<strong>Step 2: Replanning.</strong> The agent evaluates the first results and decides it needs more targeted evidence. This is where agent reasoning adds value over a single RAG call." },
    { type: "action", text: '<code>search_literature("algorithmic curation filter bubbles echo chambers")</code>',
      annotation: "<strong>Step 2: Refined search.</strong> The agent uses a different query informed by terms it found in the first observation." },
    { type: "observation", text: '[1] "Guess et al. (2023): Experimental evidence from Facebook and Instagram suggests that removing algorithmic curation had minimal effects on political attitudes and polarization, challenging the echo chamber hypothesis..." (score: 0.81)<br>[2] "Levy (2021): A randomized experiment on Facebook news feed shows that exposure to counter-attitudinal content can reduce affective polarization, but effects are modest and short-lived..." (score: 0.74)',
      annotation: "<strong>Step 2: New evidence.</strong> This search found experimental studies that complicate the narrative. The agent now has both observational and experimental findings." },
    { type: "thought", text: "I now have evidence on both sides. Let me extract the key findings from the most important passage to make sure I represent it accurately.",
      annotation: "<strong>Step 3: Verification.</strong> Rather than jumping to a conclusion, the agent extracts structured claims from a specific passage. This is the faithfulness discipline from Module 4." },
    { type: "action", text: '<code>extract_findings("Guess et al. (2023): Experimental evidence from Facebook and Instagram suggests that removing algorithmic curation had minimal effects...")</code>',
      annotation: "<strong>Step 3: Extraction.</strong> The agent uses the extraction tool to pull structured, citable claims." },
    { type: "observation", text: "Findings:<br>\u2022 Removing algorithmic curation on Facebook/Instagram did not significantly change political attitudes<br>\u2022 Effect on affective polarization was minimal<br>\u2022 Authors conclude that algorithms are \"not the main driver\" of polarization<br>Source: Guess et al. (2023), experimental design",
      annotation: "<strong>Step 3: Structured evidence.</strong> The extraction provides specific, verifiable claims. The agent can now synthesize from structured findings rather than loose impressions." },
    { type: "thought", text: "I have enough evidence to answer the question. The literature shows a nuanced picture: concerns about echo chambers exist but experimental evidence suggests algorithmic curation is not the primary driver. I should present both sides with citations.",
      annotation: "<strong>Step 4: Deciding to stop.</strong> The agent judges it has sufficient evidence. Stopping too early produces thin answers; stopping too late wastes tokens." },
    { type: "answer", text: 'Recent political science research presents a <strong>nuanced and contested picture</strong> of social media\'s role in polarization. Observational studies document patterns consistent with echo chambers and ideological sorting on platforms like Twitter (Barber\u00e1 2023), and scholars have identified mechanisms including algorithmic curation, low barriers to expression, and the public visibility of political conflict (Settle 2022, Kubin & von Sikorski 2024).<br><br>However, experimental evidence complicates this narrative. Guess et al. (2023) found that removing algorithmic curation on Facebook and Instagram had minimal effects on political attitudes or affective polarization, suggesting algorithms are "not the main driver." Similarly, Levy (2021) found that exposure to counter-attitudinal content had only modest, short-lived effects.<br><br>The emerging consensus is that social media may <strong>reinforce and accelerate</strong> pre-existing polarization trends rather than causing them independently (Kubin & von Sikorski 2024).',
      annotation: "<strong>Final Answer.</strong> The agent synthesizes across three search results and one extraction, citing specific sources. Every claim is traceable to an observation from a tool call." }
  ];

  var visibleSteps = 0;
  var typeLabels = { thought: "Thought", action: "Action", observation: "Observation", answer: "Final Answer" };

  function render() {
    var html = '<div class="widget-wrap">';
    html += '<div class="widget-eyebrow">Interactive visualisation</div>';
    html += '<div class="widget-title">The ReAct Loop in Action</div>';
    html += '<p class="widget-intro">Watch an agent work through a research question step by step. Click to advance. Notice how each Thought leads to an Action, each Observation informs the next Thought, and the loop continues until the agent has enough evidence.</p>';

    html += '<div class="rl__question"><div class="rl__question-label">Research question</div><div class="rl__question-text">"' + question + '"</div></div>';
    html += '<div class="rl__trace">';
    for (var i = 0; i < visibleSteps; i++) {
      var s = steps[i];
      html += '<div class="rl__step"><div class="rl__step-dot rl__step-dot--' + s.type + '"></div>';
      html += '<div class="rl__step-card rl__step-card--' + s.type + '"><div class="rl__step-label rl__step-label--' + s.type + '">' + typeLabels[s.type] + '</div>' + s.text + '</div></div>';
    }
    if (visibleSteps === 0) html += '<div style="text-align:center;padding:3rem 1rem;color:var(--text-muted,#6B7280);font-size:0.88rem;font-style:italic;">Click "Start agent" to begin.</div>';
    html += '</div>';

    var isDone = visibleSteps >= steps.length;
    html += '<div class="rl__controls">';
    if (!isDone) html += '<button class="rl__btn rl__btn--primary" id="rl-next">' + (visibleSteps === 0 ? 'Start agent' : 'Next step') + '</button>';
    if (visibleSteps > 0) html += '<button class="rl__btn rl__btn--ghost" id="rl-reset">Reset</button>';
    html += '<span class="rl__step-counter">' + visibleSteps + ' / ' + steps.length + '</span></div>';

    html += '<div class="rl__legend">';
    html += '<span class="rl__legend-item"><span class="rl__legend-dot" style="background:#2E5090;"></span> Thought</span>';
    html += '<span class="rl__legend-item"><span class="rl__legend-dot" style="background:#8B6914;"></span> Action</span>';
    html += '<span class="rl__legend-item"><span class="rl__legend-dot" style="background:#2D7D46;"></span> Observation</span>';
    html += '<span class="rl__legend-item"><span class="rl__legend-dot" style="background:#6D28D9;"></span> Final Answer</span>';
    html += '</div>';

    if (visibleSteps > 0) {
      html += '<div class="rl__annotation">' + steps[visibleSteps - 1].annotation + '</div>';
    } else {
      html += '<div class="rl__annotation">The agent receives a research question and must decide how to answer it using only the tools available: search, extract, summarize, and calculate. It cannot browse the web or access information beyond its tool set.</div>';
    }
    html += '</div>';

    root.innerHTML = html;

    var nextBtn = document.getElementById('rl-next');
    if (nextBtn) nextBtn.addEventListener('click', function () { visibleSteps++; render(); });
    var resetBtn = document.getElementById('rl-reset');
    if (resetBtn) resetBtn.addEventListener('click', function () { visibleSteps = 0; render(); });
  }
  render();
})();
</script>

<!-- =================================================================== -->
<!-- 2. AGENT FAILURE MODES                                              -->
<!-- =================================================================== -->
<script is:inline>
(function () {
  var root = document.getElementById("failure-modes-root");
  if (!root) return;

  var modes = [
    {
      name: "Hallucination on Missing Data", brief: "The corpus lacks the answer, but the agent fabricates one anyway",
      icon: "\uD83D\uDC7B", color: "#9E3A38", bgColor: "#FDF0F0", borderColor: "#E8AAAA",
      example: { bg: "#FDF0F0", labelColor: "#9E3A38", label: "Example trace",
        lines: [
          { label: "Thought", color: "#2E5090", bg: "#EDF2FA", text: "I need to find unemployment data for France in 2019." },
          { label: "Action", color: "#8B6914", bg: "#FDF8EC", text: 'search_literature("France unemployment 2019")' },
          { label: "Observation", color: "#2D7D46", bg: "#EDF7F0", text: "[No relevant results. Top result: general EU economic governance, score: 0.31]" },
          { label: "Answer", color: "#9E3A38", bg: "#FDF0F0", text: '"According to the literature, France\'s unemployment rate in 2019 was approximately 8.5%..." \u2190 <strong>Fabricated.</strong> The corpus contains no unemployment statistics. The agent drew from training data while appearing to cite the corpus.' }
        ]
      },
      safeguard: "<strong>Minimum evidence threshold.</strong> If no search result scores above 0.5, the agent should acknowledge the gap instead of synthesizing. Add a check: if all retrieval scores are below threshold, return \"The corpus does not contain information on this topic.\""
    },
    {
      name: "Format Drift", brief: "The model stops following the Thought/Action structure",
      icon: "\uD83C\uDF00", color: "#8B6914", bgColor: "#FDF8EC", borderColor: "#E8D49A",
      example: { bg: "#FDF8EC", labelColor: "#8B6914", label: "Example trace (step 4 of 6)",
        lines: [
          { label: "Thought", color: "#2E5090", bg: "#EDF2FA", text: "I've found some relevant material. Let me search for more." },
          { label: "Action", color: "#8B6914", bg: "#FDF8EC", text: 'search_literature("democratic backsliding causes")' },
          { label: "Observation", color: "#2D7D46", bg: "#EDF7F0", text: "[Results about institutional erosion, executive power, etc.]" },
          { label: "???", color: "#8B6914", bg: "#FDF8EC", text: '"Based on what I\'ve found so far, it seems like democratic backsliding is primarily caused by..." \u2190 <strong>Format broken.</strong> The model started writing a conversational answer instead of using Thought/Action format. The parser cannot extract a tool call or final answer.' }
        ]
      },
      safeguard: "<strong>Format enforcement.</strong> When the parser fails, inject a message reminding the model of the exact format. Larger models drift less frequently. Keeping conversations shorter (fewer steps) also helps."
    },
    {
      name: "Redundant Loops", brief: "The agent repeats the same actions without making progress",
      icon: "\uD83D\uDD04", color: "#6D28D9", bgColor: "#F3EEFF", borderColor: "#C9B5F5",
      example: { bg: "#F3EEFF", labelColor: "#6D28D9", label: "Example trace (steps 3-5)",
        lines: [
          { label: "Action 3", color: "#8B6914", bg: "#FDF8EC", text: 'search_literature("populism causes")' },
          { label: "Action 4", color: "#8B6914", bg: "#FDF8EC", text: 'search_literature("causes of populism")' },
          { label: "Action 5", color: "#8B6914", bg: "#FDF8EC", text: 'search_literature("what causes populism")' },
          { label: "Result", color: "#6D28D9", bg: "#F3EEFF", text: '\u2190 <strong>Three nearly identical queries.</strong> The agent used 3 of its 6 steps retrieving the same passages with slightly different wording, wasting tokens and budget.' }
        ]
      },
      safeguard: "<strong>Step budget awareness.</strong> After step N/2, inject a message: \"You have used 3 of 6 steps. Summarize what you have found so far and plan your remaining steps carefully.\" This forces the agent to take stock and diversify."
    },
    {
      name: "Cascading Errors", brief: "An early mistake propagates through the entire chain",
      icon: "\u26A1", color: "#2E5090", bgColor: "#EDF2FA", borderColor: "#B8CCE8",
      example: { bg: "#EDF2FA", labelColor: "#2E5090", label: "Example trace",
        lines: [
          { label: "Action 1", color: "#8B6914", bg: "#FDF8EC", text: 'search_literature("impact of trade agreements") \u2190 Wrong query for a question about sanctions' },
          { label: "Observe", color: "#2D7D46", bg: "#EDF7F0", text: "[Returns passages about free trade agreements, not sanctions]" },
          { label: "Action 2", color: "#8B6914", bg: "#FDF8EC", text: "extract_findings([passage about trade agreements])" },
          { label: "Answer", color: "#9E3A38", bg: "#FDF0F0", text: '"The literature suggests that international trade agreements are the primary mechanism..." \u2190 <strong>Each step is locally correct</strong> (extraction is faithful to the passage), <strong>but the chain is globally wrong</strong> because the first search was off-target.' }
        ]
      },
      safeguard: "<strong>Provenance verification.</strong> After the agent finishes, check whether each claim in the answer connects to an observation that actually addresses the original question. This catches cases where the chain drifted away from the research question."
    }
  ];

  var activeIdx = null;

  function render() {
    var html = '<div class="widget-wrap">';
    html += '<div class="widget-eyebrow">Interactive reference</div>';
    html += '<div class="widget-title">Agent Failure Modes</div>';
    html += '<p class="widget-intro">Agents fail in ways that are harder to detect than single-call errors. Click each card to see a concrete example and the safeguard that addresses it.</p>';
    html += '<div class="fm__cards">';

    for (var i = 0; i < modes.length; i++) {
      var m = modes[i];
      var isActive = activeIdx === i;
      var cls = isActive ? ' fm__card--active' : '';
      var bs = isActive ? 'border-color:' + m.borderColor + ';' : '';

      html += '<div class="fm__card' + cls + '" style="' + bs + '">';
      html += '<div class="fm__card-header" data-idx="' + i + '">';
      html += '<div class="fm__card-icon" style="background:' + m.bgColor + ';color:' + m.color + ';">' + m.icon + '</div>';
      html += '<div class="fm__card-label"><div class="fm__card-name">' + m.name + '</div><div class="fm__card-brief">' + m.brief + '</div></div>';
      html += '<div class="fm__card-chevron">\u25B6</div></div>';

      html += '<div class="fm__card-detail">';
      var ex = m.example;
      html += '<div class="fm__example" style="background:' + ex.bg + ';">';
      html += '<div class="fm__example-label" style="color:' + ex.labelColor + ';">' + ex.label + '</div>';
      for (var j = 0; j < ex.lines.length; j++) {
        var l = ex.lines[j];
        html += '<div class="fm__trace-line"><span class="fm__trace-label" style="color:' + l.color + ';background:' + l.bg + ';">' + l.label + '</span><span>' + l.text + '</span></div>';
      }
      html += '</div>';
      html += '<div class="fm__safeguard"><div class="fm__safeguard-label">Safeguard</div>' + m.safeguard + '</div>';
      html += '</div></div>';
    }

    html += '</div></div>';
    root.innerHTML = html;

    root.querySelectorAll('.fm__card-header').forEach(function (hdr) {
      hdr.addEventListener('click', function () {
        var idx = parseInt(hdr.getAttribute('data-idx'), 10);
        activeIdx = activeIdx === idx ? null : idx;
        render();
      });
    });
  }
  render();
})();
</script>

